#include<stdio.h>

int main()
{
   int x;
   printf("enter the number\n");
   scanf("%d",&x);
   float y=1,g=0;
   while(g==0)
    {
      y=(y+x/y)/2;
      if(y*y==(float)x)
        {g=1;}
    }
   printf("the root is %f",y);
}ount=0;
          if(prime>=0.0)
            {prime1=prime;}
          for(u=2;u<prime1;u++)
            { if(prime1%u==0)
               {count=1;}
            }
          if(count==0)
           { if((float)prime1==prime)
              {printf("the number '%f' is panaitopal prime\n",prime);}
           }
        }
    }
}
